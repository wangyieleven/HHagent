# 合合助手抽屉 / 工作台 QA · 2026-09-13

本节覆盖用户指定的 `site/`，下方 2026-09-03 记录仅为历史版本。

final result: passed

验收范围：两种形态的布局与切换、顶部边界、420px 抽屉宽度及现有本地问答入口。不是原图逐像素差分通过，也不代表全部业务流程或真实联网能力验收。

## 参考与截图

- 本轮四色品牌参考：`/var/folders/gq/ck9883m15bbbr9qmm9ty788m0000gn/T/codex-clipboard-3515d4b9-d972-42ba-80ce-566125ff2d25.png`（252×228）；已复制为 `site/images/hehe-ui/four-color-mark.png`。实机截图与原图再次并列检查；卡片按用户指定的绿、红、蓝、黄排列，同时保留标记本身左绿、上黄、右蓝、下红的结构。
- 原图：`audit-output/2026-09-13-drawer-workbench/reference-drawer.png`（336×672）、`reference-workbench.png`（1672×941）。
- 工作台：同目录 `workbench.png`，浏览器 1684×1060，实际助手位于官网导航下方。对照时排除官网导航，比较助手内部的三栏比例。
- 420px 抽屉：`drawer-420.png`，浏览器 1440×960；抽屉宽420，高864，顶部90，与导航下沿相同。最终实际窗口复验 `drawer-final.png`，1967×1354，抽屉420×1225.06，顶部122.94。
- 窄窗口：`narrow-window.png`，390×844，面板378px，无主区域横向溢出；输入区完整可见。此宽度官网导航隐藏，因此助手顶部为0。
- 浏览器 devicePixelRatio=1；保存完整浏览器截图，未将含官网区域的截图当成同尺寸原稿。抽屉从原参考约326px加宽至420px，属于用户明确要求的尺寸调整。
- 同一次图片检查中同时打开两张原图及两个实现截图，并结合卡片字体、宽度、换行的 DOM 测量作局部检查。

## 对照修正

- 修复原首页缺失 `chatContext`、`scenarioRow` 导致问答引擎初始化退出；挂载节点保留为隐藏兼容容器。
- 移除旧启动问候占据消息区的问题，恢复参考图欢迎首页。
- 修复旧工作台样式覆盖发送按钮颜色、字号和尺寸；新增样式限定在当前助手。
- 工作台左上品牌替换为四色标记、“合合助手”和 Beta；品牌本身作为返回首页入口，并移除下方重复的“合合助手”导航项。
- 四张首页卡片按绿、红、蓝、黄重新映射，发送键使用同一四色标记，形成一致的品牌色线索。
- 首轮抽屉326px；按用户中途反馈调整为420px，卡片、推荐问题、输入字号与间距同步调整。
- 修复资源卡片描述因1px宽度不足额外断行；最终卡片高134px。
- 修复旧表单脚本对已移除 `chatCards` 无条件绑定事件产生的报错。

## 五个视觉检查面

| 项目 | 结果 |
| --- | --- |
| 字体 | 采用本地中文无衬线字体，抽屉标题20px、说明14px、问题13px；工作台标题43px并按窗口缩小。 |
| 布局 | 同一面板从右侧展开，工作台显示左导航、中心问答、右推荐。导航下沿动态测量，输入框固定在面板底部。 |
| 色彩 | 浅蓝背景；服务卡从左到右为绿、红、蓝、黄的轻渐变，标题与图标使用对应深色；发送键使用四色品牌标记。 |
| 图像 | 复用现有吉祥物；左上品牌和发送键使用用户提供的252×228四色标记；背景、城市横幅为生成素材，功能图标使用 Remix Icon 与 Bootstrap Icons。 |
| 内容 | 还原三卡抽屉、四卡工作台、推荐、常用与输入工具；附件JSON未读取、未接入。 |

## 实际交互验证

- Logo打开侧边抽屉；展开/收起工作台均在原页面完成。
- 左上“合合助手 Beta”品牌按钮从历史对话正确返回首页，返回后侧栏无错误高亮；导航中“合合助手”重复项数量实测为0。
- 输入“测试切换时保留输入”，展开和收起后内容相同。
- 最新实测抽屉最终宽度420px，回到工作台后面板宽1522px；工作台与官网导航交界同为95.875px，主区和整体壳层 `scrollWidth === clientWidth`。
- 四色图片以252×228原始尺寸成功加载，工作台内图片加载失败数为0，刷新后的浏览器错误日志为空。
- 发送“平台有哪些创新服务？”，消息与本地回复在展开后完整保留。
- 窄窗口展开、关闭通过，关闭后 `aria-hidden=true`。
- 所有助手图片加载成功。最新刷新后未出现新的错误；日志仍保留刷新前的旧版初始化错误，不误报为本次新错误。
- 新增JS及改动表单JS `node --check`通过。仓库原静态安全检查通过，但该脚本检查的是 `mirror/site`，不作为本次 `site` 的运行验收依据。

## 保留的细节差异

- 图标为可用图标库版本，原图的3D高光、手写宣传语笔触和背景波纹并非原始独立素材，存在细节差异。
- 工作台导航的非核心页面提供本地轻量状态；收藏、数据空间等不代表完整后台功能。
- 本轮未推送、部署；预览服务继续在本地4173端口运行。

---

# 历史记录：合合助手视觉与交互 QA · 对话优先版

final result: passed

验证日期：2026-09-03。此结论仅覆盖本次对话优先 UI 改造和下列已执行的代表性交互，不代表所有业务分支、真实 AI 或生产系统通过验收。

## 当前依据与旧结论替代

用户最新要求为“像一个智能体助手，而不是点选的系统”。此前“恢复六类能力及双列卡片常驻首屏”的 QA 方向已被替代，不再作为当前交付标准。

本次沿用合合品牌、透明吉祥物和首页侧栏，以最新两张京京截图的轻量首屏、粉蓝底色、白色回答和珊瑚色提问作为视觉参考；业务内容仍是数智创新服务。

## 最终对照

- [最终欢迎页](</Users/wangyi/vibe coding/HHagent/audit-output/2026-09-03-ui-implementation/45-delivery-welcome-mobile.jpg>)：402×894 CSS px，截图同尺寸；已与用户欢迎页参考图放入同一次图像输入检查。
- [最终问答页](</Users/wangyi/vibe coding/HHagent/audit-output/2026-09-03-ui-implementation/46-delivery-policy-mobile.jpg>)：402×894 CSS px，截图同尺寸；已与用户问答参考图放入同一次图像输入检查。
- 用户原图是 1200×2670 px，与 402×894 比例接近；对比的是同类欢迎/问答状态，内容不同，不声称逐像素复刻或每行同位置。
- [320px 窄屏](</Users/wangyi/vibe coding/HHagent/audit-output/2026-09-03-ui-implementation/38-agent-welcome-320.jpg>)：320×720，标题和按钮可见；DOM 实测助手宽 320px、无横向溢出、可见按钮/输入无左右越界。
- [桌面欢迎](</Users/wangyi/vibe coding/HHagent/audit-output/2026-09-03-ui-implementation/40-agent-desktop-welcome.jpg>)：1440×900，448px 侧栏，首页大吉祥物保留；这是过程截图，最近提问的头像文字前缀已在最终脚本修正。
- [桌面表单](</Users/wangyi/vibe coding/HHagent/audit-output/2026-09-03-ui-implementation/41-agent-form-desktop.jpg>)：1440×900，主动展开后 600px，字段输入 16px，无消息区横向溢出。

参考原文件：
1. /var/folders/gq/ck9883m15bbbr9qmm9ty788m0000gn/T/codex-clipboard-e23cf140-162f-4cd0-94a2-867655524e21.png
2. /var/folders/gq/ck9883m15bbbr9qmm9ty788m0000gn/T/codex-clipboard-0bf37f8a-1d13-4ddc-a882-2d01202863b4.png

## 五个视觉检查面

| 检查面 | 最终结果与边界 |
| --- | --- |
| 字体与层级 | 问候标题、回答正文、辅助提示层次清晰；正文保留舒适行距。表单输入实测 16px。320px 不出现被截断的标题或按钮。 |
| 布局与密度 | 首屏只保留问候、3 条示例问题与输入区；六类能力、21 场景移入更多；普通回答不再夹带常驻工作台。底部 3 个小工具不扩成多排导航。 |
| 颜色与装饰 | 浅粉蓝渐变背景、白色回答、珊瑚色提问，与参考氛围一致。用户气泡改深为 #bd5261，白字对比度约 4.62:1；不是全站 WCAG 认证。 |
| 图像与品牌 | 头像及欢迎图复用 assistant-mascot.png；本轮 sips 核验 972×1103、hasAlpha=yes。无裁切蓝底、无圆形底板；首页大吉祥物保留。 |
| 内容与状态 | 使用合合业务内容，不照搬个人政务问题；材料/资源示例标注原型；无虚构联网搜索数量、语音识别或审批完成状态。 |

有意保留的差异：网站桌面右侧栏而非独立手机 App；合合标题而非“京京”；独立发送键而非未实现的麦克风；不复制四个分类标签与长列表，以满足用户“现在有点太多了”的要求。

## 发现、修复、复验

| 问题 | 修复及实测结果 |
| --- | --- |
| 首屏常驻模块过多 | 场景、能力、身份和方案归入更多；首次欢迎截图只显示 3 条问题及输入区 |
| 办理一开始直接展开长表单 | 先连续追问；预约前三轮实测表单字段数量为 0 |
| 每轮回复积累同样的按钮 | 只保留最新 intake 操作区；多轮预约实测数量为 1 |
| 快捷换话题可能填进旧草稿 | 快捷提问清除待收集状态；预约中点击“问政策”直接得到政策回答 |
| 算力问题仍反问数据还是算力 | 优先回答明确的算力材料要点；继续申请保留算力资源默认值 |
| 用户气泡白字对比度不足 | 从 #cf6875 改为 #bd5261，约 3.57:1 提升至 4.62:1；最终 DOM 与截图复验 |
| 最近提问混入用户头像“我” | 仅取消息气泡文字；排除手机号/联系字段，最近列表最多 3 条 |
| 政策示例按钮传入 PointerEvent | 按钮回调无参调用，避免把鼠标事件作为问题字符串 |

## 交互验证记录

最终对话版重点执行：

- 首屏示例、换话题工具、“更多”、折叠服务范围、新对话均可操作。
- 更多中读取到 21 个服务场景（另有 1 个展开/收起控制），能力没有删除。
- 预约参观连续收集时间/时段/人数、机构/联系人/电话、目的七项；核对页与输入一致。
- 预约核对提交后生成本地回执，进度页使用同一编号，显示待受理（演示）。
- 资源材料回答可进入申请准备；主动打开表单才提示法人演示身份，切换后表单展开。
- 桌面表单 600px 与正常聊天 448px 切换；手机单列；320px 与 402px 均检查无消息区横向溢出。
- 同步验证 15 段非空内联脚本和 3 个独立助手模块语法解析。

本轮前半段还执行了空表单校验、返回/草稿恢复、组合资源名传递、链式前置审批、动态资源类型、发布场景核对值、项目附件阻断、单选不重复、医疗路径上下文、补材料等代表分支。它们的截图是工作台阶段的历史证据，不能代替最新对话版每个分支的重测。

## 仍需后续验证的边界

未做任意自然语言理解、真实 AI/RAG/联网来源、真实上传/受理/审批、跨端历史同步。输入框暂为文本，不提供未实现的语音按钮。未完成实机软键盘、Safari、多浏览器、200% 缩放、完整读屏与所有分支自动化测试。

静态镜像原有业务 API 告警不在本轮处理范围；本报告不宣称全站控制台零错误。当前结论为本地前端原型视觉与代表性交互验收通过。

[详细实施及 21 场景报告](</Users/wangyi/vibe coding/HHagent/合合助手-对话优先UI优化实施报告-2026-09-03.md>)

## 2026-09-13 卡片配色更新

- 参考：用户上传的 codex-clipboard-f2b2a2d7-002a-43ff-9318-3da2ab3594bc.png（2089 × 753）。
- 本地预览：http://127.0.0.1:4173/，展开工作台首页；浏览器截图已检查。
- 本次范围：四张服务卡片配色。依次采用橙黄、青绿、珊瑚红、亮蓝；浅色渐变底、同色边框、右下角斜向色带及同色箭头。箭头调整为左对齐。
- 字体与内容：保留现有字体、字号、文案和图标库；卡片内容无截断。
- 布局：保留现有卡片尺寸和响应式网格，适配当前页面密度；不按参考图的独立画布尺寸放大。
- 视觉检查：四种颜色顺序与参考一致，标题、图标、边框及箭头配色一致，背景色带没有遮挡内容。现有点击处理未修改。
- 本轮没有发现范围内需要修复的 P0/P1/P2 问题。
- final result: passed
