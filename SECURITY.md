# HHagent 安全说明

## 支持范围

HHagent 当前是静态交互原型，不是生产业务系统。安全修复主要保证：默认本地演示不主动连接真实认证、业务 API 和访问日志接口，不在浏览器存储中保留明显的认证凭证与个人敏感字段，并降低动态富文本注入风险。

这些前端保护不能替代服务端鉴权、数据权限、审计、内容安全、文件安全、接口幂等和隐私治理。

## 演示环境规则

1. 不输入真实账号、密码、令牌、身份证号、手机号或未脱敏业务材料。
2. 页面中的“提交成功”“回执”“审批进度”均为本地模拟，不产生真实业务效力。
3. Demo 模式默认关闭实时 API、认证和访问日志；只有部署侧显式注入运行时配置后才允许启用。
4. 演示结束后，通过助手“更多 → 清除本次演示数据”清理草稿、记录、身份缓存和创新方案。
5. 对外演示只发布经过审核的静态目录，不直接暴露整个仓库。

## 仓库公开与历史内容

仓库当前为公开可见。若业务文档、采集结果、网页镜像或第三方素材仅限内部使用，应在 GitHub 仓库设置中改为 Private。

仅在新提交中删除文件或地址并不会清除 Git 历史。历史内容需要单独评估，并在必要时使用经过审批的历史清理流程；清理后还需轮换任何可能暴露的凭证。当前仓库未发现有效密钥，但历史资料和内网地址仍应按组织安全要求处理。

## 上线前最低要求

- 将助手拆分为独立、可构建、可测试的前端应用；
- 区分 Demo、Sandbox、Production 配置与部署权限；
- 使用后端会话或安全 Cookie，不在前端保存原始 Token；
- 所有业务提交由后端校验权限、状态、参数和幂等键；
- 文件上传经过类型、大小、病毒、恶意内容和数据权限检查；
- 模型输入输出经过敏感信息、意识形态、违法违规和提示注入检测；
- 记录操作审计，并建立留存、删除和用户授权规则；
- 完成依赖漏洞、静态代码、动态应用、越权和隐私合规测试。

## 报告问题

请不要在公开 Issue 中粘贴真实凭证、个人信息、内部地址或业务数据。通过组织批准的私密安全渠道提供最小复现信息，并在确认泄露后立即轮换相关凭证。
