# 合合助手原型与 V6 验证记录

验证日期：2026-09-13。

## 实现范围

- 保留蓝白主色和四色服务卡片；发送按钮与深度思考、联网搜索同排。
- 访客、个人、企业三类演示身份；历史按日期分组，身份间记录分开。
- 工作台等宽办理与问答联动，支持字段修改、缺项校验、版本核对和明确确认。
- 独立我的办理页面支持筛选、搜索、草稿恢复及同编号模拟补正。
- 目录使用 2026-09-02 快照；问答和字段提取采用规则与示例。未接入真实模型、身份、上传、正式提交或审批接口，记录仅保留本次访问。

## 验证证据

- `NODE_PATH=/private/tmp/hh-workbench-tests/node_modules node work/secretary-report/test-workbench.cjs` 通过。覆盖目录条件继承、614/569/38/583 计数、角色隔离、四项必填、确认失效、防重提交、补正同号、已提交记录不可直接修改等。
- 新模块、外壳及加载脚本的 JavaScript 语法检查通过；`git diff --check` 通过。
- 浏览器 1366×900 下左右业务区域均为 540.5px；主要业务路径走通，无横向溢出，浏览器错误日志为空。390×844 仅做基础堆叠与溢出检查，不代表完整移动端验收。
- 官网创新服务、需求反馈、我要提供、政策及第五批通知已核对；参观实时场次和登录后表单未完成实时核验，原型及报告已注明边界。
- V6 共 19 页，最终 `render-checked/page-1.png` 至 `page-19.png` 全部逐页检查；正文、架构、截图、图注、表格及来源页无明显裁切或重叠。保留 V5 源文件。
- 项目既有静态安全脚本指向历史 `mirror/site`，不能作为新 `site/` 模块验证证据。

未执行推送或部署。文档渲染用 PDF/PNG 为内部排版检查文件，正式交付为 deliverables 下的 V6 DOCX。
